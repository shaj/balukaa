name: Publish
# on:
#   workflow_dispatch:
#     inputs:
#       run_tests:
#         description: "CI pipeline name"
#         required: false
#         default: "run_tests.yml"

# on:
#   release:
#     types: [created]

on:
  push:
    branches:
      - "master"
      - "OTUS"

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Repo 
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Declare some variables
        id: vars
        shell: bash
        run: |
          echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
          echo "::set-output name=sha_short::$(git rev-parse --short HEAD)"
      - name: Another step
        run: |
          echo "Branch: ${{ steps.vars.outputs.branch }}"
          echo "Sha: ${{ steps.vars.outputs.sha_short }}"

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: ./
          file: ./Dockerfile
          push: true
          # tags: ${{ secrets.DOCKER_HUB_USERNAME }}/balukaa:latest
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/balukaa:${{ steps.vars.outputs.branch }}.${{ steps.vars.outputs.sha_short }}

      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  # echo:
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: foo
  #     url: www.google.com
  #   steps:
  #     - name: Echo message
  #       run: echo '${{ github.event.inputs.run_tests }}'